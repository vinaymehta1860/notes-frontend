import React from 'react';

class HomePage extends React.Component {
  // constructor(props) {
  //   super(props);
  // }

  render() {
    // Inside this fragment, have two self made components, OWNER & SHARED
    // Only import owner notes if the user has notes which he ownes
    // Only import shared notes if the user has shared notes with him
    return (
      <React.Fragments>
        
      </React.Fragments>
    );
  }
}

export default HomePage;